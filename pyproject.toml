[project]
name = "interpiped"
version = "0.1.0"
dependencies = [
  "fastapi>=0.95",
  "uvicorn>=0.22",
  "pydantic>=2.0",
]
requires-python = ">=3.12"

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-q"
